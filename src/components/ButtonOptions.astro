---
var types = ["Portfolio Desarrollo", "Portfolio Hacking Red Team"];
var href = ["/desarrollo", "/hacking"];
---

<div class="flex flex-wrap items-center justify-center gap-6">
  {types.map((n, i) => (
    <a
      href={href[i]}
      class="relative px-6 py-3 text-lg font-medium text-[#4a8d92] border-2 border-[#61A2A7] rounded-xl 
             transition-all duration-300 ease-out hover:bg-[#78bdc2] hover:text-black hover:shadow-lg 
             focus:outline-none focus:ring-2 focus:ring-[#61A2A7]"
      role="button"
      aria-label={n}
    >
      {n}
    </a>
  ))}
</div>
